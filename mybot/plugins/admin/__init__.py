"""Administrative plugins for managing users and settings.

This subpackage groups admin-related handlers such as broadcasting messages,
user management, and withdrawal processing.  Keeping these modules in a
subpackage avoids name conflicts with the core admin panel plugin
(`admin_panel.py`).
"""

